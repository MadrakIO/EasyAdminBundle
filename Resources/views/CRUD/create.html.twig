{% extends 'MadrakIOEasyAdminBundle:CRUD:layout.html.twig' %}

{% block headline %}
  {% if title is defined %}{{ title }}{% else %}Create{% endif %}
{% endblock %}

{% block crud_content %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Create</button>
    {{ form_end(form) }}
{% endblock %}

